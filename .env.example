APP_ENV=development
# Required in production; generate with: python -c "import secrets; print(secrets.token_urlsafe(48))"
SECRET_KEY=
DATABASE_URL=
REDIS_URL=
UPLOAD_FOLDER=
FORCE_HTTPS=false
# Enable only when the app is directly behind one trusted reverse proxy.
TRUST_PROXY_HEADERS=false
